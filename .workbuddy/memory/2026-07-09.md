# 2026-07-09 iCode 工作日志

## 主要工作

### 1. 国内 Provider 模型更新 (2026.07)
- DeepSeek: V3/R1 → V4-Flash/V4-Pro (1M 上下文, prefix cache)
- 智谱: GLM-4-Plus → GLM-5
- Kimi: moonshot-v1 → K2.7 Code / K2.6
- 火山方舟: 豆包 Pro/Lite → Seed 2.1 Pro/Turbo
- 腾讯混元: Pro/Lite → TurboS/T1
- 华为盘古: 4.0 → 5.0 Pro/Code
- SCNET: 升级 128K/256K 上下文
- TokenPlan 新增 Currency 字段

### 2. v0.2 P0 阻塞修复
- TUI ↔ Engine 桥接 (chatCallback.OnSend 调用 Engine.Send)
- exec 命令完整实现 (非交互模式)
- auth 命令 CRUD (set/list/show/delete)
- Fetch 工具真正 HTTP GET
- 修复 config 路径不匹配 bug
- 修复 flag shorthand 冲突 (-l → -L)

### 3. TUI 重写为 Claude Code 级别
- 全屏 raw terminal 模式
- 底部固定状态栏 (model · tokens · cost · cache · project)
- Token 颜色分级 (白/黄/橙/红)
- 角色图标渲染
- 双击可运行 (检测非终端环境，退出时等待 Enter)

### 4. 桌面版
- 纯 HTML 桌面应用 (零依赖)
- Vite 前端构建 (React + TypeScript)
- Go 后端直接服务前端 (同一端口)
- desktop.exe 一键启动器 (启动服务 + 打开浏览器)
- npm 镜像配置 (npmmirror.com, ELECTRON_MIRROR)

### 5. Claude Code 功能补齐
- EditTool: 精确字符串替换 (old_string → new_string)
- GitDiffTool / GitCommitTool / GitStatusTool
- GrepTool 重写为纯 Go (跨平台, 不依赖 Unix grep)
- parseJSONArgs / parseBoolArg: 正式 JSON 参数解析
- /compact: 上下文压缩
- /export: 导出 markdown
- /diff: git diff 展示
- /resume: 恢复会话 (placeholder)
- ICODE.md 项目上下文文件 (对标 CLAUDE.md)

### 当前状态
- 11 个工具 (Bash/Read/Write/Edit/Grep/Glob/LS/Fetch/GitDiff/GitCommit/GitStatus)
- 9 个 Provider, 22 个模型
- CLI: 14MB, 桌面启动器: 5.9MB
- go vet 通过
- Git commit: 2322b2f

### 6. 权限门控接入对话引擎 (Task #46) — 已完成
- `Engine` 注入 `*permission.Gate`，`NewEngine` 新增 gate 参数；`app.Bootstrap` 已传参
- 工具执行前调用 `gate.Check`：plan 阻断写入/命令/编辑，yolo 全放行(危险命令除外)，agent 需确认
- 双路径权限解析：
  - CLI 路径：`Engine.SetPermissionHandler` 指向 `tui.PromptPermission`（agent 模式弹出终端内授权框，y=允许 / a=本次会话全允许 / n|Ctrl+C=拒绝），由流式 goroutine 直接读键
  - 服务端路径：permHandler 为 nil 时引擎经 SSE 下发 `EventPermission` 事件，桌面弹对话框，POST `/api/permission/respond` 回传决定（`engine.SetPermissionResponse` 唤醒阻塞的引擎 goroutine）
- 新增 `types.EventPermission` + `types.PermissionReq`；gate 增强 `isReadOnly`/`buildPrompt`（识别 edit/git_*）
- 修复 TUI 渲染死锁隐患：`render()` 改为先持 `t.mu` 快照、再用独立 `renderMu` 写终端；`AppendStream`/`EndStream`/`compact` 去掉嵌套锁
- exec 命令设 permHandler 自动放行（非交互自动化不阻塞）

### 7. 桌面端对接真实后端 API (Task #47) — 已完成
- 重写 `desktop/index.html`：移除假 `generateResponse`，改用 `fetch` + SSE 消费 `/api/chat`
- 流式渲染：text 增量追加到 assistant 气泡；`[Tool: name]` 包裹行路由到对应工具气泡；`tool_use` 显示工具标签+参数
- 权限对话框（modal）：监听 `EventPermission`，Allow/Deny/AllowAll 三按钮 + Y/N/A 快捷键，POST `/api/permission/respond`
- 头部 Model 下拉 + Mode 标签（点击切换并 POST `/api/permission/mode`）；Stop 按钮（POST `/api/chat/stop`）；连接状态指示
- `/api/chat` 增强：接收 `model`/`provider`，会话缺失时自动创建，变更时更新 session
- 服务端 `handleFrontend` 改为优先服务 `desktop/index.html`（自包含，免构建），其次 `desktop/dist`
- 新增 `/api/permission/respond` 端点

### 验证
- `go build ./...` + `go vet ./...` 均通过
- 服务端 smoke test：/api/health、/api/models、/api/sessions(自动建会话)、/api/permission/mode、/api/permission/respond、/api/chat/stop 全部正常
- /api/chat SSE 正常回传 error 事件（因无有效 API Key 返回 401，证明管线通畅）
- CLI 行模式 `/exit` 正常退出
- 桌面 JS `node --check` 语法通过
- 重新构建 icode.exe / desktop.exe 成功

### 8. OpenRouter auto/free 模型核实与修复
- 核实：openrouter `auto` 与 `free` 已在代码中配置（provider 目录 DefaultModels、CLI printDefaultModels 表、桌面 appStore.ts 均含）
- 问题：`free` 是虚构的 OpenRouter 模型 ID（OpenRouter 没有名为 "free" 的模型），真正对话时会被 API 拒绝（model not found）；`auto` 是真实的 OpenRouter 路由模型
- 修复：`internal/llm/provider/openrouter/openrouter.go` 给 provider 包一层薄包装器，请求时把 `free` 解析为真实免费模型 `qwen/qwen2.5-coder-32b-instruct:free`；`auto` 原样透传；别名表 `modelAliases` 可扩展
- 验证：/api/models 正确列出 `auto`(OpenRouter Auto) 与 `free`(OpenRouter Free)；go build/vet 通过，重建二进制成功
- 注：config.yaml 的 `models: []` 为空属正常，引擎按 provider 目录回退解析，不影响这两个模型可用

### 9. 严格按用户给定数据执行（撤销映射）
- 用户明确指示"严格执行我给定的数据"：不要自作主张把 `free` 替换为 qwen 免费模型
- 已撤销第 8 步的包装器/别名映射，`openrouter.go` 的 `New` 恢复为直接返回 `openai_compat.New(...)`，`auto`/`free` 作为模型 ID **原样透传**给 OpenRouter，不做任何 ID 替换
- 教训：用户给定的模型 ID/数据应严格保留，不要"自作聪明"做等价替换；若认为数据有问题，先告知而非擅自改
- 重建 icode.exe / desktop.exe 成功（注意：构建前需杀掉占文件的残留 icode.exe server 进程，否则 Windows 文件锁导致 -o 覆盖失败）

### 10. 四代理研究 + 参照项目落地（Clippy/ClaudeCode/Reasonix → iCode）
- 用 4 个子代理：研究 ClawCode、开源 Claude Code、Reasonix，+ 第 4 个审核代理核对 iCode 覆盖度
- 结论：权限三模式✓、ICODE.md✓、多Provider✓、/resume✓、自动压缩✓、hooks✓、MCP(stdio/sse)✓ 已具备；缺失：CLI启动ASCII LOGO、主题token体系、/context可视化、词级diff、权限框超时；桌面缺右上下文面板/分段状态栏/OKLCH配色；重型(经验学习闭环/多角色编排)本轮不做

### 11. CLI 启动 ASCII LOGO（Task #53）
- 用 pyfiglet(managed venv) 生成 "iCODE" standard 字体，存入 `internal/tui/logo.go`(raw string literal)
- 全屏 raw 模式：logo 进入 TUI `bannerLines()` 顶部（cyan 着色）
- 行模式(非TTY)：`chatCmd` 在 `t.Run()` 前打印 `tui.Logo()` 纯文本
- 验证：行模式 `printf "/exit" | icode chat` 正确显示 figlet LOGO + 横幅

### 12. 美观设置界面（Task #54）
- CLI：`config` 子命令重写为真设置面板（boxed、CJK宽度感知对齐）+ setter：`icode config [model|provider|mode|lang|theme|diff] <v>`、`icode config providers`
- TUI：新增 `/config` slash 显示面板
- chat 启动回退到 config 默认值（flags 缺省时读 `~/.icode/config.yaml`）
- `config.go` 新增 `Defaults{Model,Provider,Mode}` 字段 + `DefaultPath()` + `LoadOrCreate()`
- 桌面端：设置 modal（模型/提供商/权限模式/主题/语言），PUT `/api/config` 持久化

### 13. 桌面端四区布局重做（Task #55，对标 Reasonix）
- 重写 `desktop/index.html`：OKLCH 感知均匀配色 token（dark/light 主题切换）
- 四区：标题栏(多会话 tab + 新会话/设置) + 左侧栏(会话/提供商) + 中线程(流式/工具卡/思考) + 右上下文面板(token 分段计量条 + 缓存率/成本/模式/模型)
- 底部分段状态栏（模型/缓存%/成本/Tokens/模式，点击模型/模式打开设置）
- 设置 modal、授权卡 ApprovalCard(Allow/Deny/AllowAll + Y/N/A)、slash/@ 自动补全弹层
- 会话 tab：新建/切换/关闭/恢复(`/api/sessions/{id}` GET)、左侧栏同步
- 服务端 `handleConfig` PUT 扩展：持久化 defaults.model/provider/mode、tui.theme 到磁盘（`config.Save`）
- 验证：server 冒烟 新UI 正常服务、/api/config GET/PUT(ok)、/api/models(22)、JS `node --check` 通过、二进制重建成功
- 注：重型架构能力(经验学习闭环/多角色编排)按用户"继续"默认采用推荐路线，本轮留后续

### 14. 双击可执行文件自动运行（无参数默认进聊天）
- 需求：直接点击 icode.exe 即可运行，而不是仅打印 help 后关闭窗口
- `cmd/commands.go`：把 chatCmd.RunE 的启动逻辑提取为共享函数 `startChat(provider, model, mode string) error`，chat 子命令与其复用
- `cmd/root.go`：rootCmd 新增 `RunE`，无子命令（如双击）时调用 `startChat(provider, model, "")` 直接进入聊天；`--help`/`--version` 仍正常（Cobra 优先级更高）
- 验证：`icode --help` 正常；无参数运行（管道 stdin 非 TTY）正确打印 ASCII LOGO + TUI 提示符并退出(exit=0)；双击时终端为 TTY 进入 raw 全屏聊天
- 两个入口：icode.exe 双击→CLI 聊天；desktop.exe 双击→启动本地服务并在浏览器打开图形界面(GUI)
- go build 通过，重建 icode.exe / desktop.exe 成功

### 15. 双端 API Key 可视化设置
- 痛点：CLI 仅 `auth` 命令行可设 key，桌面端设置 modal 无 key 字段；后端无保存 key 端点（ProviderCfg.APIKey 是 json:"-"，安全不回传前端）
- 后端 `internal/server/server.go`：新增 `PUT /api/config/key`（provider+api_key+api_base 持久化到 config.yaml）与 `GET /api/config/keys`（返回各 provider 的 key_set 状态 + api_base，绝不回显明文密钥）；并注册路由
- 桌面端 `desktop/index.html`：设置弹窗(⚙)新增"API 密钥"区——列出所有 provider，每个含 key 输入框/可选 Base URL/保存按钮/已配置·未配置徽标；支持新增自定义 provider（名称+Base URL+Key）；配套 CSS（modal 可滚动、key-list、toast）与 JS（loadKeys/renderKeyList/saveKey/addKey/toast），openSettings 时自动加载
- CLI：config 命令新增 `key` 子命令（`icode config key <provider> <key>` 保存、`icode config key` 列出状态）；renderConfigPanel 加 key 提示行；startChat 检测当前 provider 缺 key 时打印配置提示（auth set 命令 / 桌面端设置）
- 验证：go build/vet 通过；重建 icode.exe/desktop.exe；桌面 JS(vm)语法 OK；后端冒烟 GET/PUT /api/config/keys 正常且明文 key 不回显；测试 provider 已用 auth --delete 清理，config 文件无残留

### 16. 大模型名字可定制 + 斜杠命令/界面完善 + 桌面原生窗口
- 需求：①扩展可定制范围（模型名字可改/自定义模型）②桌面版有自己的 UI 不要从浏览器启动 ③完善 CLI/桌面斜杠命令与界面
- 模型可定制（config + 后端）：
  - `internal/config/config.go`：`ModelCfg` 扩展字段 `ID/Name/BaseURL/ContextWindow/MaxOutput/FreeTier/Custom`；新增 `ModelKey`/`UpsertModel`/`DeleteModel`/`FindModel`/`ModelDisplayName` 辅助方法
  - `internal/server/server.go`：`handleListModels` 合并内置模型 + 自定义模型（带 name 覆盖）；新增 `GET /api/config/models`、`PUT /api/config/model`（upsert，custom=true 持久化）、`DELETE /api/config/model?id=`（路由统一为 `/api/config/model` 按 method 分发）
- 桌面端（设置弹窗"模型"区）：列出自定义模型（改名/删除）、新增自定义模型（选 provider + model_id + 显示名）；send()/saveSettings 从 MODELS 解析 provider+model_id（自定义模型可属不同 provider）；SLASH 列表扩展并新增 UI 触发（`/config`打开设置、`/keys`滚动到密钥、`/model`聚焦模型、`/provider`聚焦提供商，在 send() 拦截）；newModelProvider 合并自定义 provider
- CLI：`config` 命令新增 `model` 子命令（`list` / `add <p> <id> [name]` / `rm <id>`）；TUI `handleSlash` 新增 `/provider /keys /models /config /history /theme` 命令并完善中文 `/help`
- 桌面原生窗口（不从浏览器启动）：`cmd/desktop_launcher/main_windows.go` 引入 `github.com/zserge/lorca`（纯 Go，CGO_ENABLED=0 可编译）；启动后端后用 lorca 打开系统 Chrome/Edge 的 DevTools 原生窗口加载 UI；找不到 Chrome 时回退 `msedge --app=URL`（Edge 应用窗口，仍非浏览器标签），再回退系统浏览器；窗口关闭时 kill 后端进程
- 环境约束：本沙箱无 gcc/MinGW（CGO_ENABLED=0）、无 WebView2；真正的 webview_go/Wails 原生嵌入需 gcc+WebView2，无法在此编译验证，故采用 lorca（纯 Go）实现"自有窗口、不从浏览器启动"
- 验证：go build ./... + go vet ./... 通过；重建 icode.exe/desktop.exe；桌面 JS(vm)语法 OK；后端冒烟 PUT/GET/DELETE /api/config/model（自定义模型正确合并进 /api/models，配置无残留）；lorca 在 CGO 关闭下成功编译
- 注意：`go.mod`/`go.sum` 新增 lorca 依赖（golang.org/x/net）

### 17. 桌面原生窗口改 WebView2（彻底不依赖浏览器）+ OpenRouter free 模型 ID 改名
- 背景：用户认为上一轮 lorca 仍是用 Chrome/Edge 引擎（本质还是浏览器）打开，要求"继续修改成不用浏览器打开"
- 桌面窗口方案替换：移除 `github.com/zserge/lorca`，改用纯 Go 的 `github.com/jchv/go-webview2`（CGO_ENABLED=0 可编译，直接调用 Windows WebView2 COM 控件创建**真正的原生窗口**——无地址栏、无标签页、不是浏览器）
  - `cmd/desktop_launcher/main_windows.go` 重写为：启动后端 → `webview2.NewWithOptions(WebViewOptions{Title:"iCode", Width:1200, Height:820, Center:true, DataPath:<LOCALAPPDATA>/icode/webview})` → `w.Navigate(url)` → `w.Run()`；窗口关闭后 defer kill 后端
  - WebView2 运行时缺失时 `NewWithOptions` 返回 nil → 用 `user32.MessageBoxW`（纯 Go，golang.org/x/sys/windows）弹出错误框说明需安装 WebView2 运行时，**绝不退回浏览器**
  - 已 `go mod tidy` 清除 lorca；`go build/vet ./**` CGO 关闭通过；重建 icode.exe/desktop.exe 成功
- OpenRouter 模型 ID 改名（用户明确要求"把openrouter的free模型ID改为openrouter/free"）：
  - `internal/llm/provider/openrouter/openrouter.go`：`DefaultModels()` 中 `auto`→`openrouter/auto`、`free`→`openrouter/free`（与用户最初指定的 `openrouter/auto` / `openrouter/free` 一致，并顺带修复默认模型 `openrouter/auto` 此前在注册表只有 `auto` 导致解析不到的隐患）
  - 验证：`ResolveModel` 用精确匹配 `m.ID==modelID`（registry.go:102），引擎 `Model: sess.ModelID`（engine.go:239）直接透传，openai_compat 第310行 `"model": req.Model` 透传 —— 发往 API 正是字面量 `openrouter/free`；`/api/models` 已返回 `openrouter/auto` 与 `openrouter/free`
- 环境约束：本沙箱无 WebView2 运行时，故无法在此 runtime 验证窗口渲染（仅编译验证）；用户机若已装 Edge，WebView2 运行时通常已内置

### 18. CLI 输入自动补全 + 多语言(中/繁/英) + 主题/设置增强
- 需求：①CLI 版过于简陋，输入 `/` 时自动提示预设命令及中文解释 ②设置增加更多可定制项（主题、语言；语言支持简体中文+繁体中文）
- 新增 `internal/tui/i18n_tui.go`：TUI 自带翻译表 `tuiStrings`（zh-CN/zh-TW/en，含 ac.title/ac.hint/banner.hint/perm.*/status.gen/lang.*/theme.*/sc.* 与 18 条 slash 命令 desc）；`slashDefs` 作为斜杠命令单一数据源（驱动 /help 与自动补全）；`palettes`（dark/light 调色板）；方法 `tstr`(按 t.lang 取值，回退 zh-CN)、`c`(主题感知取色)、`paint`(主题感知，替换原 cXxx 常量)
- TUI 结构体新增 `lang/theme/acOpen/acItems/acIdx`；`Config` 增加 `Lang/Theme`；`commands.go:startChat` 传入 `cfg.Language`/`cfg.TUI.Theme`
- 自动补全（仅 raw 模式）：输入为空或 `/` 时显示命令列表+本地化解释面板（输入行上方）；`Ctrl+P/N` 上下选择、`Tab` 补全（补 `命令名 + 空格`）、`Esc` 关闭；`updateSuggestions/acceptSuggestion/autocompleteLines/allSuggestions` 实现；render() 渲染 acLines 并把其行数计入 reserved
- 主题应用：`/theme <auto|dark|light>` 实际切换并持久化（`persistSetting` 写 config.yaml；auto/非 light 渲染为 dark，light 用更深的 accent 色适配浅色终端背景）；`/lang <zh-CN|zh-TW|en>` 切换并持久化，全 UI（banner 提示、/help、权限提示、状态 generating、/config）即切换语言
- 设置增强：`renderConfigPanel` 新增 Syntax highlight(on/off)、Server(host:port)、Languages/Themes 提示、TUI 实时切换提示；`runConfigSet` 支持 `syntax`(on/off)；`/config` 展示 Language/Theme/Diff/Syntax；`config` 命令 Long 帮助补充 syntax
- 验证：go build ./... + go vet ./... 通过；重建 icode.exe/desktop.exe；管道会话验证 zh-TW/en/zh-CN 下 /help 与 banner 提示全部本地化正确（繁体"輸入你的需求，或 /help 檢視指令..."、英文 "Show this help" 等），无 panic；`config`/`config lang zh-TW`/`config theme light`/`config syntax off` 均正常保存


### 19. 桌面版状态栏/配色对标 Reasonix（用户："底部信息和绝大多数设置可参考照搬reasonix桌面版"）
- 派研究代理核实 Reasonix 桌面 UI（Tauri2+React）：底栏字段=API/baseUrl+连接、缓存命中%、Tokens、本次成本、作业数、工作目录、模型、余额、主题切换（无 git 分支）；设置页只有单一全局 baseUrl+apiKey + 硬编码 DeepSeek 预设卡片，**没有**厂商→模型 CRUD（用户要的厂商→模型是本项目的扩展）；布局 CSS Grid 行 36/36/1fr/26、列 244/1fr/320；调色板全 OKLCH，强调色橙红 `oklch(68% 0.16 38)`、紫 `oklch(72% 0.14 295)`、radius 8px；标题栏有真正窗口最小化/最大化/关闭
- 桌面状态栏重写对标 Reasonix：`模型 · 厂商 · 模式 · 缓存 · Tokens · 成本 · 作业 · 目录 · [spacer] · 连接 · 主题切换`；移除原"会话"段（tab 已承载）；新增 `#segJobs`(工具进行中计数，SSE `tool_use`+1 / `done`归0)、`#segCwd`(工作目录)、`#segTheme`(◐/◑ 切换深/浅并 PUT /api/config 持久化)
- 后端 `handleStatus` 新增返回 `cwd`（os.Getwd），前端 boot() 拉取填入状态栏"目录"
- 配色对标 Reasonix：`--accent` 改为橙红 `oklch(68% 0.16 38)`、`--reasoning` 紫 `oklch(72% 0.14 295)`、`--radius` 8px；主题切换按钮去默认 button 外观
- 窗口最小化/最大化/关闭：go-webview2 默认 `WS_OVERLAPPEDWINDOW` 已原生提供，无需自定义标题栏
- 验证：go vet ./... 通过；重建 icode.exe(21.4MB)/desktop.exe(9.8MB)；桌面 JS `node --check` 通过；`/api/status` 冒烟返回含 `cwd:"E:\\icode"`；修复 updateContext 残留 `$('sbSession')`(已删元素)导致崩溃的隐患
- 注：替换被锁定的 icode.exe 需先 `Stop-Process -Id <pid>`（Windows 文件锁）；Replace-Item/Remove-Item 被 safe-delete 钩子拦截，用 `Copy-Item -Force` 覆盖；本机另运行着 reasonix-desktop.exe（参考样本）
