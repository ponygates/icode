---
name: pptx-deck
description: 从大纲生成幻灯片（.pptx）：展业演示、产品讲解、培训课件、方案汇报。用 Python(python-pptx) 产出结构清晰的演示文稿。
triggers:
  - 生成PPT
  - pptx幻灯片
  - 展业演示
  - 培训课件
  - 方案汇报
---

# 幻灯片生成

当用户要求生成 .pptx 演示文稿时，按以下流程产出。

## 流程
1. **先出大纲**：每页 = 标题 + 要点（3-5 条），让用户确认页序与内容。
2. **生成方式**：`python -c` + python-pptx（或 python-pptx 脚本文件）。
3. **版式规范**：
   - 第 1 页：封面（大标题 + 副标题 + 讲者/日期）。
   - 内容页：标题 + 项目符号要点，每页信息量克制（≤5 条）。
   - 末页：总结/行动号召 + 联系方式（如需）。
   - 配色：简洁两色（主色 + 强调色），避免花哨。

## 输出要求
- 生成后返回**文件路径**与总页数。
- 若缺 python-pptx，给出安装命令（`pip install python-pptx`）。
- **合规**：保险/金融演示涉及收益/费率时标注"以正式条款为准"，不承诺收益；数据需用户提供，不虚构。
- 结合 image_gen 可先出封面图再嵌入（可选）。
